---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getLangFromUrl, useTranslations } from '../../i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<!doctype html>
<html lang={lang}>
	<head>
--- a/src/pages/fr/index.astro
+++ b/src/pages/fr/index.astro
@@ lines 14-18
  <head>
-   <BaseHead title={t('site.title')} description={t('site.description')} />
+   <BaseHead title={t('site.title')} description={t('site.description')} />
+   <link
+     rel="alternate"
+     href={`https://votredomaine.com/fr${Astro.url.pathname}`}
+     hreflang="fr"
+   />
+   <link
+     rel="alternate"
+     href={`https://votredomaine.com${Astro.url.pathname.replace(/^\/fr/, '')}`}
+     hreflang="en"
+   />
  </head>
	<body>
		<Header />
		<main>
			<h1>{t('home.title')}</h1>
			<p>{t('home.description')}</p>
		</main>
		<Footer />
	</body>
</html> 