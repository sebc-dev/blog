# Epic 1: Initialisation et Infrastructure du Blog (Core Platform Setup)

**Version:** 0.1
**Date :** 2025-05-11
**Statut :** Ébauche

## 1. Objectif de l'Epic

L'objectif principal de cet Epic est de mettre en place l'ensemble de l'infrastructure technique de base nécessaire pour héberger et servir le blog technique bilingue. Cela inclut la configuration du serveur VPS, l'installation des technologies de conteneurisation, la mise en place du reverse proxy, la structure initiale des projets frontend et backend, et les configurations de base pour la base de données et le thème visuel. À la fin de cet Epic, nous devrions avoir un "shell" fonctionnel de l'application, prêt à recevoir le contenu et les fonctionnalités plus spécifiques.

*(Réf PRD : "Epic 1: Initialisation et Infrastructure du Blog (Core Platform Setup) - Goal: Mettre en place la plateforme de blog avec la stack technique spécifiée (Astro pour le frontend/MDX, Spring Boot pour les API de comptage backend, PostgreSQL), configurer l'hébergement VPS (OVH, Debian, Docker, Traefik), le thème de base responsive avec TailwindCSS et DaisyUI, et les fonctionnalités essentielles non liées au contenu (sécurité de base avec Traefik, performance initiale).")*

## 2. User Stories (Fonctionnelles et Techniques)

| ID    | En tant que... (Rôle)          | Je veux... (Action/Besoin)                                                                                                | Afin de... (Bénéfice/Objectif)                                                                                                                               | Priorité (MVP) | Notes Techniques / Critères d'Acceptation Clés (pour Architecte)                                                                                                                                                                                                                                                                                                                                                                                                                        |
| :---- | :----------------------------- | :------------------------------------------------------------------------------------------------------------------------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **INFRASTRUCTURE & SETUP** |                                |                                                                                                           |                                                                                                                                                              |                |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| E1-S01 | Administrateur Système (Admin) | Configurer le serveur VPS OVH (Debian) avec les bases de sécurité (SSH par clé, `ufw`, `fail2ban`).                            | Disposer d'un environnement serveur sécurisé pour héberger l'application.                                                                                    | Haute          | - Accès SSH par clé uniquement, `PasswordAuthentication no`.<br>- `ufw` configuré (ALLOW SSH, HTTP, HTTPS).<br>- `fail2ban` installé et actif.<br>- Système Debian à jour.<br>*(Réf : `docs/operations/runbook.md`)* |
| E1-S02 | Admin                          | Installer et configurer Docker Engine et Docker Compose sur le VPS.                                                         | Pouvoir exécuter les applications conteneurisées de manière isolée et reproductible.                                                                       | Haute          | - Docker Engine et Docker Compose (plugin v2) installés et fonctionnels.<br>- L'utilisateur de déploiement peut exécuter les commandes `docker` (via groupe `docker` ou sudo).<br>*(Réf : `docs/operations/runbook.md`)* |
| E1-S03 | Admin                          | Mettre en place Traefik comme reverse proxy dans un conteneur Docker sur le VPS.                                            | Gérer le trafic entrant HTTPS, obtenir et renouveler automatiquement les certificats SSL (Let's Encrypt), et router les requêtes vers les services frontend et backend. | Haute          | - Conteneur Traefik fonctionnel.<br>- Points d'entrée HTTP/HTTPS configurés.<br>- Configuration ACME pour Let's Encrypt fonctionnelle (e-mail admin, volume pour `acme.json` avec permissions 600).<br>- Routage de base vers des services de test (placeholder) fonctionnel.<br>- Dashboard Traefik sécurisé si activé.<br>*(Réf : `docs/architecture/architecture-principale.md`, `docs/operations/runbook.md`)* |
| E1-S04 | Admin                          | Configurer la base de données PostgreSQL dans un conteneur Docker, avec persistance des données.                            | Fournir un stockage de données relationnel pour les métriques de l'application backend.                                                                    | Haute          | - Conteneur PostgreSQL fonctionnel (version spécifiée dans `teck-stack.txt`).<br>- Variables d'environnement pour utilisateur, mot de passe, nom de DB configurées (`POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`).<br>- Volume Docker pour la persistance des données (`/var/lib/postgresql/data`).<br>- Base de données accessible depuis le réseau Docker interne par le service backend.<br>*(Réf : `docs/architecture/architecture-principale.md`, `docs/data-models.md`)* |
| E1-S05 | Développeur (Dev)              | Initialiser le projet monorepo Git avec la structure de base pour les répertoires `frontend/`, `backend/`, et `docs/`.        | Avoir une structure de projet claire et organisée dès le début.                                                                                             | Haute          | - Dépôt Git créé.<br>- Structure de dossiers conforme à `docs/project-structure.md`.<br>- Fichiers `.gitignore` et `.editorconfig` de base.                                                                                                                                                                                                                                                                                                                                                           |
| **FRONTEND (ASTRO)** |                                |                                                                                                           |                                                                                                                                                              |                |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| E1-F01 | Dev                            | Initialiser un nouveau projet Astro dans `frontend/` avec les dépendances de base (TypeScript, TailwindCSS, DaisyUI, PNPM). | Avoir un projet frontend Astro fonctionnel avec la stack UI définie.                                                                                         | Haute          | - Projet Astro créé (version spécifiée dans `teck-stack.txt`).<br>- TailwindCSS et DaisyUI installés et configurés (`tailwind.config.js`, `postcss.config.cjs`, fichier CSS global).<br>- PNPM configuré comme gestionnaire de paquets.<br>- Page d'accueil Astro basique affichable.<br>*(Réf : `docs/project-structure.md`, `docs/ui-ux/ui-ux-spec.md` pour les thèmes DaisyUI)* |
| E1-F02 | Dev                            | Configurer le thème DaisyUI de base (clair/sombre) avec la palette de couleurs et la typographie définies.                  | Assurer une identité visuelle cohérente dès le début et respecter les spécifications UI/UX.                                                                  | Haute          | - Thèmes DaisyUI (clair et sombre, ex: "customLight", "customDark") configurés dans `tailwind.config.js` ou `app.css` avec les couleurs HSL spécifiées dans `docs/ui-ux/ui-ux-spec.md` (Section 4.1).<br>- Polices (Inter, JetBrains Mono) configurées dans `tailwind.config.js` et importées.<br>- Le plugin `@tailwindcss/typography` est installé et configuré de base.<br>- Un sélecteur de thème (clair/sombre) fonctionnel est présent.                                                                         |
| E1-F03 | Dev                            | Créer le layout de base (`BaseLayout.astro`) incluant le header et le footer globaux (structure vide).                      | Avoir une structure de page cohérente pour l'ensemble du site.                                                                                               | Haute          | - `BaseLayout.astro` créé avec slots pour le contenu.<br>- Composants `Header.astro` et `Footer.astro` créés (avec placeholders).<br>- Attribut `lang` sur `<html>` défini (valeur par défaut pour l'instant).<br>- Lien d'évitement "Skip to content" implémenté.<br>*(Réf : `docs/ui-ux/ui-ux-spec.md` Sections 5.1, 6.1, 6.2)* |
| E1-F04 | Dev                            | Préparer un `Dockerfile` pour l'application frontend Astro qui build le site statique et le sert via Nginx.                 | Pouvoir conteneuriser l'application frontend pour le déploiement.                                                                                          | Haute          | - `frontend/Dockerfile` créé.<br>- Stage de build utilisant PNPM pour installer les dépendances et builder Astro (`pnpm build`).<br>- Stage final utilisant une image Nginx (ou Caddy) pour servir les fichiers statiques de `frontend/dist/`.<br>- Nginx configuré pour servir en tant qu'utilisateur non-root.<br>*(Réf : `docs/operations/runbook.md`, `docs/project-structure.md`)* |
| **BACKEND (SPRING BOOT)** |                                |                                                                                                           |                                                                                                                                                              |                |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| E1-B01 | Dev                            | Initialiser un nouveau projet Spring Boot dans `backend/` avec Maven et les dépendances de base (Web, JPA, PostgreSQL, Lombok, Actuator, Liquibase). | Avoir un projet backend Spring Boot fonctionnel avec les technologies requises.                                                                           | Haute          | - Projet Spring Boot créé (version spécifiée dans `teck-stack.txt`) avec Maven.<br>- Dépendances `spring-boot-starter-web`, `spring-boot-starter-data-jpa`, `postgresql` (driver), `lombok`, `spring-boot-starter-actuator`, `liquibase-core` ajoutées au `pom.xml`.<br>- Structure de package `fr.kalifazzia.blogtechnique` (ou équivalent).<br>- Application Spring Boot démarrable.<br>*(Réf : `docs/project-structure.md`, `teck-stack.txt`)* |
| E1-B02 | Dev                            | Configurer la connexion à la base de données PostgreSQL et la gestion de base des logs pour le backend.                     | Permettre au backend de communiquer avec la base de données et de logger les informations.                                                                 | Haute          | - Configuration dans `application.yml` pour la source de données PostgreSQL (URL, utilisateur, mot de passe via variables d'environnement).<br>- Configuration Logback (`logback-spring.xml`) pour des logs JSON structurés (niveau INFO par défaut en prod, DEBUG en dev).<br>*(Réf : `docs/setup/environnement-vars.md`, `docs/observabilite/strategie-observabilite.md`)* |
| E1-B03 | Dev                            | Préparer un `Dockerfile` pour l'application backend Spring Boot.                                                            | Pouvoir conteneuriser l'application backend pour le déploiement.                                                                                         | Haute          | - `backend/Dockerfile` créé.<br>- Utilise une image de base JDK appropriée (ex: `eclipse-temurin:21-jre-jammy`).<br>- Copie le fichier JAR packagé (`backend/target/*.jar`) dans l'image.<br>- Définit un utilisateur non-root pour exécuter l'application.<br>- Expose le port de l'application (ex: 8080).<br>*(Réf : `docs/project-structure.md`)* |
| **DOCKER COMPOSE** |                                |                                                                                                           |                                                                                                                                                              |                |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| E1-D01 | Dev                            | Créer un fichier `docker-compose.yml` de base pour orchestrer les services (frontend, backend, db, traefik) en dev local.     | Faciliter le lancement et la gestion de l'environnement de développement local complet.                                                                      | Haute          | - `docker-compose.yml` (et `docker-compose.override.yml` pour dev) créé à la racine.<br>- Services définis : frontend (build depuis Dockerfile), backend (build depuis Dockerfile), db (image PostgreSQL), traefik (image Traefik).<br>- Réseaux Docker configurés pour la communication inter-services.<br>- Volumes pour la persistance des données (PostgreSQL, Traefik `acme.json`).<br>- Variables d'environnement chargées depuis `.env`.<br>- Ports exposés pour le dev local.<br>*(Réf : `docs/setup/environnement-dev.md`)* |
| E1-D02 | Admin                          | Préparer un fichier `docker-compose.prod.yml` (ou l'adapter depuis le dev) pour le déploiement en production sur le VPS.      | Disposer d'une configuration Docker Compose optimisée et sécurisée pour la production.                                                                       | Haute          | - Similaire au `docker-compose.yml` de dev, mais :<br>  - Utilise des images tirées de GHCR (au lieu de les builder localement).<br>  - Pas d'exposition de ports non nécessaires (ex: port direct de la DB ou du backend).<br>  - Configuration de redémarrage (`restart: always` ou `unless-stopped`).<br>  - Configuration de Traefik pour la production (Let's Encrypt, routage des domaines, etc.).<br>*(Réf : `docs/operations/runbook.md`)* |

## 3. Critères d'Acceptation Généraux pour l'Epic

* Le VPS est configuré, sécurisé (SSH par clé, `ufw`, `fail2ban`) et Docker/Docker Compose sont installés.
* Traefik est déployé sur le VPS et capable de router le trafic HTTP/HTTPS, avec génération de certificats Let's Encrypt fonctionnelle.
* La base de données PostgreSQL est déployée dans un conteneur Docker avec persistance des données.
* Le projet monorepo est initialisé avec la structure de base (`frontend/`, `backend/`, `docs/`).
* L'application frontend Astro peut être buildée en une image Docker (avec Nginx) et servie. Une page d'accueil squelette avec le thème de base (clair/sombre) est accessible.
* L'application backend Spring Boot peut être buildée en une image Docker et démarrée. Elle se connecte à la base de données PostgreSQL et expose un endpoint de santé Actuator (`/actuator/health`) basique.
* Un fichier `docker-compose.yml` permet de lancer l'ensemble des services (frontend, backend, db, traefik) en environnement de développement local.
* Un fichier `docker-compose.prod.yml` est prêt pour le déploiement en production sur le VPS (utilisant des images de GHCR).
* La documentation de base pour la structure du projet, la configuration de l'environnement de dev, et les variables d'environnement est initiée.

## 4. Dépendances

* Accès au VPS OVH existant.
* Accès à un dépôt Git (GitHub).
* Choix des versions technologiques validés (voir `teck-stack.txt`).

## 5. Risques et Questions Ouvertes

* **Sécurisation de Traefik et de ses configurations (ACME) :** Assurer les bonnes permissions et la sauvegarde.
* **Configuration du réseau Docker pour Traefik :** S'assurer que Traefik peut découvrir et router correctement vers les services applicatifs sans les exposer directement.
* **Choix définitif du serveur web pour l'image Docker Astro (Nginx vs Caddy) :** Nginx est retenu pour l'instant. (Point de la `TODO.txt`)
* **Gestion des variables d'environnement entre local, CI, et production :** Le fichier `.env` sur le VPS est la source de vérité pour la prod.

## 6. Hors Scope pour cet Epic

* Implémentation complète des fonctionnalités métier des Epics suivants (gestion de contenu, API de métriques complète, SEO avancé, CI/CD complet).
* Création du contenu initial du blog.
* Configuration avancée de l'observabilité (monitoring détaillé, alerting).

---